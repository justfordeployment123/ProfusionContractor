# Host port exposed for the Node app
APP_PORT=3000

# MongoDB credentials and database name used by docker-compose.yml
MONGO_USERNAME=profusion
MONGO_PASSWORD=change_this_strong_password
MONGO_DATABASE=profusion
